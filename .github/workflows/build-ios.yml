name: Build iOS App

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'ios/**'
      - '.github/workflows/build-ios.yml'

jobs:
  build:
    runs-on: macos-15
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - name: Select latest Xcode
        run: |
          # List available Xcode installations
          ls -d /Applications/Xcode*.app 2>/dev/null || true
          # Use the latest available Xcode (matches installed simulator runtimes)
          LATEST_XCODE=$(ls -d /Applications/Xcode_*.app 2>/dev/null | sort -V | tail -1)
          if [ -z "$LATEST_XCODE" ]; then
            LATEST_XCODE="/Applications/Xcode.app"
          fi
          echo "Using: $LATEST_XCODE"
          sudo xcode-select -s "$LATEST_XCODE/Contents/Developer"
          xcodebuild -version

      - name: Install xcodegen
        run: brew install xcodegen

      - name: Create iOS-only project spec
        working-directory: ios/ClaudeWatch
        run: |
          cat > project-ios-only.yml << 'SPEC'
          name: ClaudeWatch
          options:
            bundleIdPrefix: com.shobhit.claudewatch
            deploymentTarget:
              iOS: "17.0"
            createIntermediateGroups: true
          settings:
            base:
              SWIFT_VERSION: "5.9"
          targets:
            ClaudeWatch:
              type: application
              platform: iOS
              sources:
                - path: "ClaudeWatch iOS"
                - path: Shared
              settings:
                base:
                  PRODUCT_BUNDLE_IDENTIFIER: com.shobhit.claudewatch
                  INFOPLIST_FILE: "ClaudeWatch iOS/Info.plist"
                  PRODUCT_NAME: Agent Watch
                  GENERATE_INFOPLIST_FILE: false
          schemes:
            ClaudeWatch:
              build:
                targets:
                  ClaudeWatch: all
              run:
                config: Debug
          SPEC
          echo "=== project-ios-only.yml ==="
          cat project-ios-only.yml

      - name: Generate Xcode project
        working-directory: ios/ClaudeWatch
        run: |
          xcodegen generate --spec project-ios-only.yml
          echo "=== Targets ==="
          xcodebuild -project ClaudeWatch.xcodeproj -list

      - name: Debug Xcode environment
        run: |
          xcodebuild -version
          xcrun simctl list runtimes 2>/dev/null || echo "No simctl runtimes"
          xcodebuild -showsdks | grep -i ios || echo "No iOS SDKs"

      - name: Build iOS app
        working-directory: ios/ClaudeWatch
        run: |
          set +e
          xcodebuild build \
            -project ClaudeWatch.xcodeproj \
            -scheme ClaudeWatch \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            -configuration Release \
            ONLY_ACTIVE_ARCH=NO \
            CODE_SIGN_IDENTITY=- \
            CODE_SIGNING_REQUIRED=NO \
            AD_HOC_CODE_SIGNING_ALLOWED=YES \
            DEVELOPMENT_TEAM="" \
            PROVISIONING_PROFILE_SPECIFIER="" \
            ENABLE_PREVIEWS=NO \
            -derivedDataPath ./build \
            > build-full.log 2>&1
          BUILD_EXIT=$?
          echo "=== BUILD EXIT CODE: $BUILD_EXIT ==="
          echo "=== LOG SIZE: $(wc -l < build-full.log) lines ==="
          echo ""
          echo "=== LAST 80 LINES ==="
          tail -80 build-full.log
          echo ""
          echo "=== ALL ERRORS ==="
          grep -i "error:" build-full.log || echo "(no error: lines found)"
          echo ""
          echo "=== ALL WARNINGS ==="
          grep -i "warning:" build-full.log | tail -10 || echo "(no warnings)"
          exit $BUILD_EXIT

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: ios/ClaudeWatch/build-full.log
          retention-days: 7
          if-no-files-found: ignore

      - name: Package IPA
        working-directory: ios/ClaudeWatch
        run: |
          APP=$(find ./build -name "*.app" -type d | head -1)
          echo "App: $APP"
          du -sh "$APP"
          mkdir Payload
          cp -r "$APP" Payload/
          zip -r AgentWatch.ipa Payload
          ls -lh AgentWatch.ipa

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: AgentWatch
          path: ios/ClaudeWatch/AgentWatch.ipa
          retention-days: 30
